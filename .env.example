# Database connection (required)
DATABASE_URL=postgresql://user:password@localhost:5432/iscreami

# CORS origins (comma-separated, default allows all)
CORS_ORIGINS=*

# Serving size for nutrition facts (grams, default 66)
SERVING_SIZE_G=66
